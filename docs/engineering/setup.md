# 本地开发环境

## 版本要求

- Node.js `22.x`
- pnpm `10.x`
- PostgreSQL `16.x`
- Redis `7.x`

## 启动步骤

1. 复制 `.env.example` 为 `.env`
2. 执行 `pnpm install`
3. 启动 PostgreSQL 与 Redis
4. 执行 `pnpm dev`

## 常用命令

- `pnpm dev`
- `pnpm build`
- `pnpm lint`
- `pnpm typecheck`
- `pnpm test`


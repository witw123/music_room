NODE_ENV=production
APP_DOMAIN=example.com
LETSENCRYPT_EMAIL=ops@example.com

PORT=3001
WEB_PORT=3000
DATABASE_URL=postgresql://music_room:replace-with-a-random-postgres-password@postgres:5432/music_room
REDIS_URL=redis://redis:6379
REDIS_MODE=single
REDIS_SENTINELS=
REDIS_SENTINEL_MASTER_NAME=
REDIS_SENTINEL_USERNAME=
REDIS_SENTINEL_PASSWORD=
REDIS_USERNAME=
REDIS_PASSWORD=
REDIS_DB=0
JWT_SECRET=replace-this-with-a-long-random-secret
AUDIT_HASH_SECRET=replace-this-with-a-separate-audit-hmac-secret
METRICS_TOKEN=replace-with-internal-prometheus-token
ADMIN_PANEL_ENABLED=true
# 管理动作默认启用；发生故障时可改为 false 并重启 server 立即关闭写操作。
ADMIN_MUTATIONS_ENABLED=true
CLIENT_TELEMETRY_ENABLED=true
CORS_ORIGINS=https://example.com
AUTH_FAKE_PERSISTENCE=false
AUTH_FAKE_PERSIST_PATH=.tmp/auth-fallback-store.json
NETEASE_ENABLED=false
NETEASE_COOKIE_ENCRYPTION_KEY=
NETEASE_DEFAULT_QUALITY=exhigh
NETEASE_MAX_IMPORT_BYTES=209715200
NETEASE_REQUEST_TIMEOUT_MS=15000
QQMUSIC_ENABLED=false
QQMUSIC_COOKIE_ENCRYPTION_KEY=
QQMUSIC_DEFAULT_QUALITY=exhigh
QQMUSIC_MAX_IMPORT_BYTES=209715200
QQMUSIC_MAX_ARTWORK_BYTES=10485760
QQMUSIC_REQUEST_TIMEOUT_MS=15000
KUGOU_ENABLED=false
KUWO_ENABLED=false
TAIHE_ENABLED=true
MIGU_ENABLED=true
BAIDU_ENABLED=false
METING_DEFAULT_QUALITY=exhigh
METING_MAX_IMPORT_BYTES=209715200
METING_REQUEST_TIMEOUT_MS=15000
METING_URL_RETRY_COUNT=1

NEXT_PUBLIC_API_BASE_URL=https://example.com
NEXT_PUBLIC_WS_URL=wss://example.com
NEXT_PUBLIC_SOCKET_PATH=/ws/socket.io
NEXT_PUBLIC_CLIENT_TELEMETRY_ENABLED=true
NEXT_PUBLIC_NETEASE_ENABLED=false
NEXT_PUBLIC_QQMUSIC_ENABLED=false
NEXT_PUBLIC_KUGOU_ENABLED=true
NEXT_PUBLIC_KUWO_ENABLED=true
NEXT_PUBLIC_TAIHE_ENABLED=true
NEXT_PUBLIC_MIGU_ENABLED=true
NEXT_PUBLIC_BAIDU_ENABLED=false
NEXT_PUBLIC_STUN_URL=stun:stun.l.google.com:19302
NEXT_PUBLIC_TURN_URL=turn:turn.example.com:3478?transport=udp
NEXT_PUBLIC_TURN_USERNAME=
NEXT_PUBLIC_TURN_CREDENTIAL=
NEXT_PUBLIC_WEBRTC_ICE_SERVERS=
TURN_ENABLED=true
# TURN 外网部署检查：
# - TURN_PUBLIC_HOST 必须是客户端可访问的公网域名或公网 IP，不得使用 localhost/127.0.0.1。
# - TURN_EXTERNAL_IP 必须是真实公网出口 IP；NAT/云主机部署时不要填内网 IP。
# - 防火墙/安全组必须开放 UDP 3478，以及 TURN_MIN_PORT-TURN_MAX_PORT 的 UDP relay 端口段。
# - 若 TURN 域名托管在 Cloudflare，记录必须设为 DNS-only，不能走橙云代理。
# - TURN_PROTOCOLS 必须保持为 udp；缓存链路不允许 relay/tcp 或 relay/tls。
# If coturn runs on the same host as the app, you can either:
#   a) Set TURN_PUBLIC_HOST=music-room.example.test (same domain, no "turn." prefix)
#   b) Set TURN_PUBLIC_HOST_USE_APP_DOMAIN=1 to reuse APP_DOMAIN directly
TURN_PUBLIC_HOST=turn.example.com
TURN_PUBLIC_HOST_USE_APP_DOMAIN=0
TURN_PUBLIC_HOST_USE_REQUEST_HOST=0
TURN_REQUEST_HOST_ALLOWLIST=example.com
TURN_REALM=turn.example.com
TURN_PORT=3478
TURN_TLS_PORT=5349
TURN_MIN_PORT=49160
TURN_MAX_PORT=49200
TURN_SHARED_SECRET=replace-with-a-turn-shared-secret
TURN_EXTERNAL_IP=your-public-ip
TURN_PROTOCOLS=udp
TURN_TTL_SECONDS=3600
GHCR_OWNER=your-org
RELEASE_TAG=sha-replace-with-full-commit-sha
ALERT_WEBHOOK_URL=https://example.invalid/alerts

POSTGRES_DB=music_room
POSTGRES_USER=music_room
POSTGRES_PASSWORD=replace-with-a-random-postgres-password
REDIS_PASSWORD=replace-with-a-random-redis-password

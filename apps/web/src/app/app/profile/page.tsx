import { ProviderAccountsPage } from "@/components/settings";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function ProfilePage() {
  return <ProviderAccountsPage />;
}

# 更新日志

本文档记录项目对外发布的重要变更。

## [未发布]

### 新增与优化

- **多人听歌完整解决方案全链路落地（第一至第四阶段）**：
  - **第一阶段（统一准入模型）**：确立“房间多人播放只认源成员分发的本地歌曲资产”唯一准则。彻底移除监听端单机偷跑播放第三方直链的旧分支；源成员离线或资产缺失时房间统一暂停播放；上线 8px 微型指示点状态徽标（`TrackDistributionBadge`）。
  - **第二阶段（自动准备资产）**：当源成员在线时，客户端引擎按优先级（本地文件 -> 本地缓存 -> IndexedDB -> 第三方下载）提取音频源，严格重算真实文件哈希，转码生成分段 Opus 资产写入 IndexedDB，并通过 `POST /rooms/:roomId/tracks/:trackId/asset` 登记至房间曲目；若房间正处于等待中，服务端自动启动统一时钟唤醒全房间同步收听；队列前 2 首在后台自动静默预准备。
  - **第三阶段（资产失效检测与异常状态降级）**：构建分片完整性校验（`verifyPlaybackAssetIntegrity`）与本地文件/目录权限丢失检测；源端主动向服务端上报不可用（`POST /rooms/:roomId/tracks/:trackId/asset/unavailable`），服务端针对当前曲目立即原子化暂停并自增 `mediaEpoch` 与广播通知；队列曲目失效保留元数据不破坏队列；在 `TrackDistributionBadge` 中为源成员提供精准文案（“权限受限”、“资产损坏”、“音频源缺失”）与一键操作（“重试准备”、“选择本地文件”恢复）。
  - **第四阶段（清理旧逻辑与体验打磨）**：清理所有旧残留的第三方临时单机播放分支、冗余参数与过时兼容层；透传 `roomId` 至所有播放器布局与队列抽屉；完成全套单元测试与端到端类型校验。

- **UI 全面清理“AI 感”发光与过度装饰，落实收敛克制规范**：
  - 全局彻底清理暗色与亮色模式中的 `--accent-glow` 霓虹发光变量与阴影类；
  - 移除按钮、分段控制器、过滤按钮上的大范围蓝色发光阴影，回归纯粹克制的 `shadow-xs` 与中性对比边框；
  - 清除各处装饰性 AI 四角星闪烁徽章（`SparklesIcon`）、彩色大虚化光斑气泡（`blur-2xl/3xl`）、发光粒子投影与假拟物元素；
  - 房间舞台背景重构为极简清爽的几何声波曲线与同心电波弧线，保留精致写实盘面，去除边缘光晕与漂浮音符杂质；
  - 房间编辑菜单、下拉菜单与详情卡片采用 100% 实体高对比表面（`bg-background-secondary border-surface-border`），彻底杜绝透底重叠造成的文字辨识障碍；
  - 修复播放进行到歌曲行时双语歌词译文意外高亮变蓝的问题，保持主歌词与译文各自清晰的语义层级。
- **产品说明落地页（`/`）工程级重构**：
  - 淘汰 AI 生成的营销模版与假拟物 Mac 窗口/自转黑胶玩具；
  - 提炼真实的四阶段端到端声学数据流架构（客户端本地沙盒 -> 服务端权威信令中心 -> WebRTC RTP Opus 实时广播 -> 监听端单一输出总线）；
  - 建立三大特色房间模式（标准协作房、点歌点播房、自由电台房）对比矩阵；
  - 展示全景歌词、本地/在线多源曲库与全平台多端特性，并提供清晰真实的本地自建启动指令。
- **顶部系统通知与公告中心重构**：
  - **默认状态收敛**：彻底移除了顶部固定的“公告 查看详情 →”文字占位，默认仅展示极简通知铃铛图标按钮（有未读时带微型指示点）；
  - **通知栏纯图标化**：移除左侧标牌的“通知”文字，仅保留纯铃铛图标；
  - **单条从最右侧循环跑马灯**：引入 CSS 容器查询单位 `100cqw` 配合单条移动关键帧 `@keyframes announcement-marquee-single`，实现每条公告严格从容器最右侧边缘入场、单条独立完整滚动、并在移出左侧后自动切换至下一条公告（或循环），彻底避免同屏重复副本或中途截断；动画按 ~60px/s 舒适平稳匀速运行，支持悬停暂停；
  - **公告中心弹窗**：对齐轻量级弹窗布局结构，支持查看历史列表、相对时间（“17天前”等）与正文详情，采用中性收敛实心背景，杜绝霓虹渐变与大圆角装饰。
- **修复 B 站歌曲加入队列后无法播放**：曲目记录转回在线曲目时只认网易云和 QQ 音乐，B 站记录一律被当成「没有在线音频」，播放器准备音源失败并跳过它去试下一首，而这一步的失败是静默的——按钮是亮的，点下去却没有任何反应，也没有提示。加入队列走的是导入链路（那里一直支持 B 站），所以表现为「加进去了但播不了」。现在 B 站的按需播放、下载到本地目录、入队状态判断与另外两个平台一致。
- **存储根目录改为登录后才要求**：此前打开 `/app`、`/rooms`、`/room` 就要先选根目录，未登录的浏览者也会被授权页挡在门外。现在门禁只看已登录会话，未登录可以照常浏览房间目录；退出登录后门禁自动让开，登录后再回来重新要求根目录。
- **Android 发布包改为正式签名**：此前 CI 用 `assembleDebug` 出包，等于把调试构建当正式版本发出去。现在 Release 工作流从仓库 Secret 解出上传密钥并执行 `assembleRelease`，`versionCode` 由版本号推导（`0.3.4 → 304`，不再手写）；缺少密钥时任务直接失败，不再静默回退到 debug 包。**签名密钥与历次调试包不同，用户升级需先卸载旧包，卸载会清空应用私有目录里的本地曲库**，详见 [Android 发布签名](./docs/deploy/android-release.md)。
- **移动端不再把原生调试日志当成报错刷屏**：发布用的 Android 包是 debug 构建，Capacitor 在 `loggingBehavior: debug`（默认值）下会把每一次原生存储调用及其结果转发进页面控制台，客户端自身的异常浮层再把这些失败项标红成「File not found / Directory not found」。这些是空仓库上对封面、歌词等可选文件与目录的存在性探测——网页端与桌面端同样会探测，只是不打印，也不代表数据丢失。现在关闭日志转发；真机排查需要原生日志时把 `loggingBehavior` 改回 `"debug"` 即可。
- **软件端根目录显示与网页端对齐**：Windows 的 `canonicalize()` 会把根目录报成扩展长度形式 `\\?\E:\game\Music Room`，设置页与歌单等处都照搬了这个前缀，现在统一显示为可读的完整路径 `E:\game\Music Room`（UNC 路径同样还原），两边都是同一行「根目录：<位置>」。网页端受浏览器限制仍只能显示所选目录名——File System Access 只暴露目录名，不提供完整路径。

## [0.3.3] - 2026-09-19

### 新增

- **B 站（哔哩哔哩）音乐全链路接入**：
  - 所有房间接入 B 站搜索栏，支持分页、独立分 P 详情页与分 P 直接播放；
  - 服务端新增 B 站音频流解析与 WBI 签名取流，修复封面防盗链破损与播放 412/500；
  - CDN 直链并发探测与故障转移，修复直连下载超时中断；
  - 支持从房间搜索直接导入 B 站音频进曲库，并接入 Alist 云存储后端作为存储层；
  - 歌词清洗与匹配策略优化，跨平台匹配命中正版录音，修复逐字/逐行歌词对不上的问题。
- **个人页（我的页）身份卡重构**：
  - 头部升级为身份卡：口味摘要（常听曲风自动生成）、累计收听时长与探索曲目统计、播放中的“正在收听”信号条；
  - 听歌画像 Tab 以口味标签为第一区块，标签升级为大号漫游入口（点击即刻开启对应偏好电台）；
  - 最常播放/常听歌手列表保留金银铜排名与单曲漫游操作；
  - 个人页 Tab 精简为「听歌画像 / 房间足迹 / 平台账号」，平台账号绑定与平台资料导入从设置页迁入；
  - “屏蔽与负反馈”降级为画像页内的折叠入口，不再占用一级 Tab。
- **设置独立路由 `/app/settings`**：
  - 纯设置项（通用、播放策略、界面、播放、通知与推送、隐私与数据、账号）从个人页 Tab 拆分为独立路由，从个人页头部齿轮入口进入；
  - 设置页头部提供“返回到我的页”按钮；
  - 移除与个人页“平台账号”Tab 重复的平台绑定与资料导入区块。
- **翻译与罗马音歌词**：支持双语歌词对齐展示，并统一各平台歌词抓取缓存。
- **发现页与听歌画像改为本地生成**：推荐与画像数据在客户端生成后由服务端持久化，口味标签、场景证据与行为标签体系重建。

### 优化与修复

- **在线播放延迟优化**：缩短起播等待，并调整底部播放器方形封面呈现。
- **播放期间 UI 渲染性能大幅优化**：
  - 移除未被消费的 `visualizerSamples` prop 链，修复其导致 BottomPlayer `React.memo` 失效、播放器子树以 30fps 空转重渲染的问题（暂停时 10fps 同步消除）；
  - 播放进度 `progressMs` 拆分出主播放器 Context，播放期间五个整页消费组件不再以 4Hz 全树重渲染，仅持久播放条订阅进度；
  - 工作区路由缓存增加 LRU 淘汰（上限 6），避免常驻隐藏页面随导航无限累积；
  - 削减大面积实时 `backdrop-filter`：底部播放条、顶栏、移动端导航栏与房间卡徽章改用近实色表面（`.workspace-surface` 磨砂卡保留）；
  - 桌面歌词改为 rAF 命令式 DOM 更新（CSS 变量逐字填充 + 直接写 transform/时间文本），消除透明置顶窗口每帧 React 重渲染；跨窗口 BroadcastChannel 与 Android 原生通道节流至 250ms，歌词对齐结果 memo 化；
  - Android 桌面歌词 overlay 帧率降至 ~30fps 且无歌词时停止重绘；
  - 缓冲/barrier 时钟轮询 100ms→250ms；分段播放引擎快照在内容无变化时跳过提交，暂停/空闲不再周期性重渲染；
  - 房间卡 `pulseRadialWave` 动画从 SVG 几何属性改为 transform 缩放，侧栏移除无效 `will-change: width`；
  - 删除无引用的死代码 `PlayerAmbientAura`。
- **Android WebView 兼容性修复**：
  - 恢复系统返回键：新增 LIFO 返回栈注册机制，返回键按「关闭浮层 → 退出房间 → 连按两次退出应用」的优先级响应，覆盖队列抽屉、沉浸式播放器、各类对话框等所有原本可用 Esc 关闭的界面；接入 `@capacitor/app` 原生插件；
  - 消除老引擎上的整站白屏：`Intl.Segmenter` 由模块顶层构造改为惰性解析并带降级实现，避免在缺失该 API 的 WebView 上于 chunk 求值阶段抛错导致整个应用无法启动。
- **移动端搜索栏输入修复**：修复中文输入法下的输入覆盖、建议列表同步与触摸提交问题，改用非受控输入 + 原生 `compositionend` 监听，搜索框不再因重渲染丢失输入。
- **歌词进度修复**：纯 LRC 也启用平滑的逐字推进，并纠正增强 LRC / QRC / YRC 三种词级时间轴的解析与相对/绝对时间判定。
- **移除构建期 Google Fonts 依赖**：构建不再需要访问外网，离线环境下可正常出包。
- **服务端播放看门狗 CPU 占用修复**：消除对全房间的周期性全量扫描导致的持续 CPU 空转。
- 存储根目录统一：桌面固定为可执行文件所在目录，Android 固定为应用 `filesDir`；网页必须显式选择根目录，移除自动 OPFS 根目录。原生读写仅开放 `.music-room`，写入失败不切换备用位置。
- 设置与歌单加载不再自动扫描音乐目录；统计使用大小索引并定向读取辅助目录，普通仓库打开不创建子目录。目录扫描仅处理变化项并批量提交索引，删除无消费者的全局目录索引生成。
- 根目录切换先释放页面与播放运行时，再切换索引并恢复目标仓库；清理保留外部原文件，回收无引用资产，失败的辅助文件保留引用。
- 保活页面增加路由与文档可见性判断：隐藏时停止房间目录轮询、画像及屏蔽记录刷新、发现页推荐生成，并取消相关网络请求；返回时按缓存新鲜度补刷新，不影响播放和听歌上报。
- 房间目录、服务端画像和屏蔽记录由已有 TanStack Query 管理，合并重复画像请求并隔离账号缓存；删除对应手写缓存，本地推荐和音频资产仍独立管理。
- 个人播放器和音频元素提升为应用级常驻：进入房间暂停并保留个人队列与位置，暂离继续由房间接管，退出后手动恢复个人播放。
- 浏览器、原生系统媒体控制及硬件媒体键统一使用活动播放器的处理器与权限；播放/暂停键正确切换，退出时释放旧监听，桌面歌词跟随同一播放归属。
- 个人在线播放不再等待封面补全、响度分析或本地目录同步；缓存元数据在后台更新，不重新绑定播放音频。
- 预加载由播放器统一管理，仅准备队列接下来的两首，并跟随顺序、随机及“下一首播放”调整；删除页面级整份歌单预加载。
- 主动点播优先于后台预取；队列调整、移除歌曲及取消准备会中止无用下载，同曲目的点播可接管正在进行的预取。
- **本地存储核对与统计的 I/O 收敛**：`filterReadableLocalFiles` 原本每次调用都自行 `LocalRepository.open()`，而一次加载会对已保存与缓存两类记录各调一次——一次 open 要建 15 个目录句柄并读一次清单，这份开销被付了两遍。现在由调用方 open 一次并共享给两次核对。缓存统计不再逐首回读音频 blob 取大小，改用已读到的元数据记录里同源的 `sizeBytes`（该字段在记录上必填，摘要记录只是剥掉了 `file`）；目录遍历的逐文件 `getFile()` 与逐记录存在性探测改为有界并发（沿用 `hardwareConcurrency - 1` 的思路，上限 4），结果按原顺序回填。
- 本地目录扫描与本地文件核对的让出策略由「每项 `setTimeout(0)`」改为按帧预算让出（约 14ms 才让一次，优先用 `scheduler.yield()`）：浏览器会把嵌套定时器钳到约 4ms，逐项让出时等待本身成了主要耗时。
- **主页按返回键直接退出**：原实现直接采信 `@capacitor/app` 的 `canGoBack`，而该值只要访问过任何页面就恒为 `true`（它反映 WebView 历史栈是否非空，而非还剩几级），导致在首页按返回键只会沿历史栈无限回退、永远退不出应用。现在首页（`/app`、`/rooms`）一律退出，其余路由才回退历史，退出仍保留“再按一次返回键退出”防误触。
- **搜索框重复的清除按钮**：`input[type="search"]` 会触发 Chromium/WebView 渲染原生的 `::-webkit-search-cancel-button`，与组件自身的清除按钮并排显示成两个 ×。已用 CSS 关闭原生搜索装饰控件。
- **桌面端更新检查对落后版本完全失明**：Tauri 窗口加载的是远端 web 包，而 `getCurrentAppVersion()` 读的是 web 包构建期注入的版本，于是「当前版本」恒等于最新部署的 web 版本，比较退化成「最新 web 版本 vs 最新 release」，永远得出「当前已是最新」——实测 0.3.2 的 Windows 安装包显示为 v0.3.3 并提示已是最新，而 0.3.1 的桌面歌词原生命令、0.3.2 的系统媒体控制（SMTC/MPRIS）都只存在于新版二进制中，用户不会收到任何提示。现在新增 `get_app_version` 命令，桌面端优先读取壳自身编译进二进制的版本（源出 `tauri.conf.json`，需与 release tag 一致），取不到时回退到 web 包版本。**此修复须随新的桌面二进制发布才生效，已安装的旧版本无法被追溯修复。**
- **补齐遗漏的版本号同步**：`packages/shared/src/version.ts` 的 `APP_VERSION` 在上次发版时漏改，仍停留在 `0.3.2`。它是 `getCurrentAppVersion()` 的兜底常量，在构建期变量不可用的场景下会让界面显示旧版本号，现同步为 `0.3.3`。

## [0.3.2] - 2026-09-05

### 新增

- **系统媒体控制全平台打通（Windows SMTC / macOS 正在播放 / Linux MPRIS / Android 媒体通知）**：
  - 桌面端（Tauri）：新增 `system_media` 原生模块（souvlaki），在 Windows（基于 WinRT SMTC 与主窗口 HWND 绑定）、macOS（Now Playing）与 Linux（MPRIS）全面展示曲名、歌手、专辑、封面与播放时间轴，支持双向系统媒体控制；
  - 移动端（Android/Capacitor）：新增 `SystemMediaControlsPlugin` 与 `mediaPlayback` 前台服务 `SystemMediaService`（MediaSessionCompat + MediaStyle 媒体通知），锁屏控件、通知抽屉与蓝牙/有线耳机线控均可播放/暂停/切歌/进度 seek；
  - 播放状态与进度 1Hz 节流推送进系统时间轴（浏览器 `setPositionState` / Tauri `MediaPosition` / Android `PlaybackStateCompat`），系统侧按播放速率自动推进，seek 后自动校准；
  - 系统命令统一回流底部播放器现有处理器，与屏幕按钮、全局媒体键同源同权限门；
  - 浏览器媒体通知封面补充 96/256/512 多档尺寸声明；Android 存在原生媒体控制插件时浏览器侧自动让位，避免重复条目。
- **真正的逐字歌词（卡拉OK 逐字时间轴）**：
  - NetEase 切换 `lyric_new` 接口获取 YRC 逐字歌词（旧 `lyric` 接口恒无 `yrc` 字段）；
  - 修复 YRC 逐字歌词首尾带有元信息 JSON 行（如作词/作曲）时未解析直接显示原始 JSON 文本的问题；
  - QQ Music QRC base64 解码透传；逐字歌词在房间与本地播放器全面生效，无逐字数据时保持 LRC 逐字匀速降级。

### 优化与修复

- **房间交互与歌词渲染性能大幅优化**：
  - 房间歌词面板与桌面歌词平滑滚动增加约 32ms 节流，消灭主线程渲染风暴；
  - 房间主舞台进度定时器放宽至 500ms，消除超高频重渲染造成的 UI 按钮与点击卡顿；
  - 退出房间操作超时优化为 150ms 极速响应，即点即退；
- 修复系统媒体命令 `seekBy` 增量（`deltaMs`）在前端断链，导致相对快进/快退无效的问题；
- QQ Music SDK 类型声明重写，消除模块内全部 22 处 `any`；
- 播放器、歌词、曲库、信令网关与两家 provider 等模块大规模结构化拆分（含 `indexeddb`、`PlaylistsWorkspacePage`、provider 页面组件化、mappers/helpers 提取），行为无变化；
- e2e 测试环境隔离移动端 header 测试 ID、关闭接口限流；修复创建房间名称占位文案与客户端组件 `use client` 指令缺失。

## [0.3.1] - 2026-08-30

### 新增

- **系统级媒体控制（SMTC / MediaSession）与全局物理媒体键响应**：
  - Windows/macOS/Linux 桌面端通过 `tauri-plugin-global-shortcut` 原生捕获全局键盘物理媒体按键（播放/暂停、上一首、下一首、停止），即使在全屏游戏或后台最小化也能自由控制播放；
  - 实时同步曲目元数据（歌名、歌手、专辑、高清封面与时间轴）至系统 SMTC 音量控制浮层与锁屏控制器；
- **系统级切歌与房间点歌 Toast 推送通知**：
  - 接入 `tauri-plugin-notification` 与 Web Notification API，后台或最小化播放切歌时自动弹出歌曲 Toast；
  - 房间点歌实时协同推送：点歌房其他成员点播新歌时自动触发系统级通知（展示点歌人、歌名、封面）；
  - 点击系统通知直接唤醒（`focus_main_window`）并前置主窗口；
  - 设置中心新增「切歌桌面通知」、「房间点歌系统通知」与「仅在后台时通知（免打扰）」细粒度开关。
- **真正的系统级桌面歌词（歌词 + 播放器一体）**：
  - 桌面端（Tauri）：新增 `toggle/hide/drag_desktop_lyrics_window` 原生命令，动态创建置顶、无边框、透明的歌词窗口并停靠主屏底部居中；歌词窗口加载独立 `/desktop-lyrics` 路由，与主窗口经 BroadcastChannel 同步播放状态，并回传上一首/播放/下一首控制命令；
  - 移动端（Android/Capacitor）：新增 `DesktopLyricsPlugin`，通过 `SYSTEM_ALERT_WINDOW` 悬浮窗实现真正的跨应用屏幕歌词；自定义 `LyricsOverlayView` 逐字卡拉OK渐变填充（Choreographer 每帧推进），支持拖动调整位置，首次开启自动引导系统悬浮窗授权；
  - 共享 `DesktopLyricsBar` 组件：封面、曲目信息、上一首/播放/下一首控制与逐词歌词同屏呈现，网易云 YRC 逐词时间轴驱动逐字填充，普通 LRC 自动按行时长逐字匀速降级；支持翻译与罗马音行；
  - 纯浏览器端悬浮歌词同步升级为合并条形态（去除胶囊折叠态），保留拖拽与位置记忆；
  - 底部播放器歌词开关三端一致：点击出现、再次点击隐藏。

### 优化与修复

- **桌面歌词体验全面重构（对齐 Apple Music / Spotify 模板）**：
  - 彻底修复子窗口状态回传监听导致的位置回弹与点击播放回弹问题；
  - 移除厚重底色边框与拖动把手图标，改为极简悬浮条并支持全栏平滑拖动；
  - 默认隐藏歌曲封面与控制栏，仅在鼠标悬停时优雅浮现精致控制工具栏；
  - 歌词逐字渐变底色与主播放器歌词风格保持完全一致；
  - 超长歌词行新增平滑跑马灯自动水平滚动，杜绝省略号截断。
- **移动端多处分段控件文字意外折行**：房间类型筛选（全部/多人互动房/点歌房/自由电台）、房间面板 Tab、曲库来源筛选、第三方源 Tab 与桌面歌词选择胶囊统一补充 `whitespace-nowrap`；
- 收藏歌手卡片取消收藏按钮移动端不可达（悬停显隐改为移动端常显）；
- 表单输入移动端字号统一 16px，规避 iOS 聚焦自动放大；
- 根视口补齐 `viewport-fit=cover`，修复 Cap 字壳/PWA 下安全区全失效的问题；
- 定制偏好弹窗层级提升并 portal 渲染，修复被底部播放器与移动端导航遮挡导致按钮不可点的问题。

## [0.3.0] - 2026-08-25

### 新增

- **全平台客户端与构建支持**：
  - 新增基于 Tauri 2.0 (Rust) 的轻量化桌面客户端工程（`apps/desktop`），支持 Windows、macOS 与 Linux 原生桌面运行；
  - 桌面客户端支持系统托盘常驻后台播放、托盘菜单控制（显示主窗口、退出）；
  - 新增 PWA（渐进式 Web 应用）标准清单 `manifest.webmanifest`，支持 PC 与移动端浏览器直接一键安装至桌面/主屏幕；
  - 新增移动端 Capacitor 接入配置（`apps/mobile`），支持 Android 与 iOS 平台容器同步；
  - 新增 GitHub Actions 多平台自动化构建发版流水线（`.github/workflows/release-desktop.yml`），支持推送版本 Tag 自动矩阵编译全平台原生安装包。

- **播放器队列列表循环优化**：
  - 调整顺序播放为全队列列表循环（List Loop），播完队列最后一首自动平滑循环至第一首，消除队列到头终止停播的问题；
  - 优化客户端壳入口直接定位至功能工作区 `https://musicroom.witw.top/app`。

### 修复

- **个人音量调节与响度均衡（音量标准化）全场景修复**：
  - 修复分段 Opus 引擎（`SegmentedOpusEngine`）在滑动音量时未即时响应的问题，完善 `setVolume` 与 `setLoudnessGain` 平滑增益调节；
  - 修复电台房与点歌房播放外部来源曲目时响度元数据未正确兜底解析导致音量均衡失效的问题；
  - 修复 WebRTC 远程接收流与本地直放流在音量滑块变动时的即时下发逻辑，确保各房间模式下成员个人音量调节与响度均衡稳定生效。

## [0.2.9] - 2026-08-12

### 文档更新

- 同步播放链路与权限模型文档：明确 `player` 权限、owner 唯一媒体源、provider offline fallback、gapless/watchdog 与单实例部署约束

### 新增

- 完善 `/app` 响应式工作区，接入 provider 搜索、歌单、收藏专辑、个人资料和设置入口
- 增加网易云与 QQ 音乐账号绑定、歌曲/歌单/专辑浏览和本地导入能力
- 增加暂离房间恢复、成员权限和主题设置

### 调整

- 房间工作区收敛为 `Library / My Playlists / Members`，共享队列由房间舞台和持久化播放器承载
- 播放链路统一为浏览器本地分段 Opus，经共享 AudioContext 输出到 WebRTC RTP Opus
- 更新开发、部署、产品和架构文档，明确单实例发布限制以及 provider 的上游依赖

### 修复

- provider 播放 fallback 不再选择来源 hash 不匹配或首尾分片不完整的本地资产
- provider 导入的本地目录写入改为不阻塞当前播放启动

## [0.2.7] - 2026-04-11

### 调整

- 桌面端默认公网入口调整为 `https://music-room.example.test`
- 统一工作区、桌面端 Tauri 配置和 Android 包内版本号到 `0.2.7`

## [0.2.6] - 2026-04-03

### 修复

- 修复 GitHub Actions 客户端发布流程未注入 `MUSIC_ROOM_PUBLIC_ORIGIN`，导致桌面端和移动端壳可能被打包到示例域名
- 为桌面端与移动端打包新增公网入口校验，缺少真实公网地址时直接构建失败，避免再次发布无效客户端

### 调整

- 统一工作区、桌面端 Tauri 配置和 Android 包内版本号到 `0.2.6`

## [0.2.5] - 2026-04-03

### 新增

- 为 FLAC 当前曲目接入基于 WebCodecs + AudioContext 的渐进式本地解码播放
- 新增 FLAC 帧解析与 PCM 引擎测试，覆盖能力探测与分片边界场景

### 调整

- 优化当前曲目分片调度，优先补齐正在播放的无损曲目并在可用时切到本地播放
- 统一工作区、桌面端 Tauri 配置和 Android 包内版本号到 `0.2.5`

### 修复

- 修复多人听歌时 FLAC 未及时切入本地缓存播放导致的卡顿问题
- 修复成员短时断线时房间在线状态和媒体链路过早抖动的问题

## [0.2.4] - 2026-03-31

### 调整

- 基于当前 `v0.2.3` 代码状态重新发布稳定版本
- 统一工作区、桌面端 Tauri 配置和 Android 包内版本号到 `0.2.4`

## [0.2.3] - 2026-03-31

### 新增

- 新增 `/app` 客户端入口页，用于承载登录、房间列表与房间内功能
- 新增客户端路由守卫，生产环境下限制浏览器直接访问应用内页面
- 新增官网下载区与 GitHub Releases 下载入口
- 新增分片调度器测试，覆盖窗口优先级、优选 peer 与超时冷却

### 调整

- 浏览器默认显示官网展示页，桌面端与移动端默认进入应用内页面
- 桌面端与移动端壳的默认加载地址改为 `/app?client=...`
- 官网、房间页与 README 文案统一收敛为面向用户的产品表达
- README 重写为项目介绍导向，弱化内部实现口径

### 性能

- 分片传输由 `base64 + JSON` 调整为二进制通道，降低 CPU 与内存开销
- 分片请求从整首扫描改为播放窗口优先调度，限制每曲目与每 peer 并发
- IndexedDB 增加精确索引，降低按单片读取时的全量扫描开销
- 播放同步从高频轮询改为事件主导加低频校正，减少无意义校准

### 修复

- 修复首次通过快捷方式启动桌面端时窗口可能被隐藏的问题
- 修复房间页、登录页与顶部导航中残留的旧入口跳转
- 修复官网和页面文案中混入实现说明式表达的问题

## [0.2.2] - 2026-03-31

### 调整

- 桌面端从 Electron 正式迁移到 Tauri 2
- 桌面端本地构建链路切换为 Rust + Tauri CLI
- GitHub Actions 桌面端构建链路切换为 Tauri Windows、macOS、Linux 打包
- Web 端桌面桥接从 `window.electron` 迁移到 Tauri `invoke`

### 修复

- 修复 Windows 下 Tauri 本地构建目录和打包脚本兼容问题
- 修复桌面安装包收集路径，统一输出到 `apps/desktop/release`

## [0.2.1] - 2026-03-31

### 新增

- 新增 Android 客户端壳与 APK 构建链路
- GitHub Release 新增 Android 安装包产物

### 调整

- 统一仓库工作区版本号到 `0.2.1`
- Release 覆盖 Windows、macOS、Linux、Android 四个平台

### 修复

- 修复 Android Gradle Wrapper 在 GitHub Actions 中缺少执行权限的问题
- 修复自动发版中 Android 构建失败的问题

## [0.1.0] - 2026-03-31

### 新增

- 提供基于 Next.js 的 Web 前端
- 提供基于 NestJS 的后端服务
- 提供桌面客户端壳
- 支持房间创建、加入与最近房间恢复
- 支持播放队列、歌单、聊天与实时同步

### 调整

- 桌面端生产模式默认远端地址改为通过 `MUSIC_ROOM_PUBLIC_ORIGIN` 注入
- 桌面客户端不内置本地 Next.js runtime
- 桌面客户端不内置本地后端服务

### 修复

- 修复 Windows 桌面端打包流程
- 修复 monorepo 场景下桌面构建兼容问题
- 修复桌面端重复打包与产物清理问题
